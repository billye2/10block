{
  "name": "10block",
  "version": "1.3.3",
  "license": "MIT",
  "private": true,
  "description": "Chrome extension: block distracting websites for 10, 30 or 60 minutes. No early unblock.",
  "scripts": {
    "test": "playwright test",
    "release": "node scripts/release.mjs",
    "release:publish": "node scripts/release.mjs --publish",
    "icons": "node scripts/gen-icons.mjs",
    "assets": "node marketing/capture.mjs && node scripts/promo.mjs && node scripts/gen-icons.mjs --store marketing/store/store-icon-128.png"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@resvg/resvg-js": "^2.6.2"
  }
}
